import { HideModalDirective } from './hide-modal.directive';

describe('HideModalDirective', () => {
  it('should create an instance', () => {
    const directive = new HideModalDirective();
    expect(directive).toBeTruthy();
  });
});
